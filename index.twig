{% extends '/layout.twig' %}
{% set theme = settings.themes.personal %}
{% set meta = metatabs.meta %}

{% block title %}{{ meta.title }} | {{ settings.title }}{% endblock %}

{% block content %}

	{% if item.elementType == 'file' %}
		
		{% include 'file.twig' %}

	{% else %}
		
		{% include 'folder.twig' %}
	
	{% endif %}

{% endblock %}