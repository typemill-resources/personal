{% extends '/layout.twig' %}

{% block title %}{{ metatabs.meta.title }} | {{ settings.title }}{% endblock %}

{% block content %}

	{% if item.elementType == 'file' %}
		
		{% include 'file.twig' %}

	{% else %}
		
		{% include 'folder.twig' %}
	
	{% endif %}

{% endblock %}